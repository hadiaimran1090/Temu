export function ConfirmModal({ onConfirm, onCancel }: { onConfirm: () => void; onCancel: () => void }) { return <div className="modal-backdrop" role="presentation" onClick={onCancel}><div className="modal" role="dialog" aria-modal="true" aria-labelledby="remove-item-title" onClick={(event) => event.stopPropagation()}><span className="modal-icon" aria-hidden="true">!</span><h2 id="remove-item-title">Remove this item?</h2><p>It will be removed from your cart. You can add it again anytime.</p><div className="modal-actions"><button className="modal-cancel" onClick={onCancel}>Keep item</button><button className="modal-remove" onClick={onConfirm}>Remove</button></div></div></div> }
